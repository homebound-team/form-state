{
  "extends": "@homebound/tsconfig/tsconfig.json",
  "compilerOptions": {
    "lib": ["esnext", "dom"],
    "types": ["vitest/globals"],
    "jsx": "react-jsx",
    // tsdown emits dist/, so tsc only type-checks
    "noEmit": true
  }
}
